var pg      = require('pg'),
    optimal = require('optimal'),
    builder = exports;

/**
 * Selects row from the database
 * 
 * Examples:
 *    
 *    builder.select('name', '*', { where: {id: 1} })
 * 
 * @param {String} table
 * @param {String} what
 * @param {Object} options
 * @param {Function} callback
 * @api public
 */

builder.select = function() {


};
